// 客户端appKey
const APP_KEY = '10145'

// 客户端ecas id
const ECAS_APP_ID = '115'

// 客户端来源id
const APP_FROM_ID = 7

// 采用相对路径，由运维配置反向代理，重定向到开放平台
const BASE_URL = '/router/rest'

export default {
  APP_KEY,
  ECAS_APP_ID,
  APP_FROM_ID,
  BASE_URL
}
